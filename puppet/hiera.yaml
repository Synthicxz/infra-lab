---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Node settings" 
    path: "nodes/%{trusted.certname}.yaml"
    
  - name: "Common settings"
    path: "common.yaml"
